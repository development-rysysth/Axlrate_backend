{
  "name": "@axlrate/scraper-orchestrator",
  "version": "1.0.0",
  "description": "Scraper orchestration service",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "amqplib": "^0.10.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "@types/amqplib": "^0.10.4",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}

